;; nbb reads this file. It does NOT read deps.edn, and the
;; .cljs here are run as bare `nbb <file>`, so a coordinate declared only
;; there is invisible to them.
;;
;; :paths and every sha below are COPIED from deps.edn, not chosen here.
{:paths ["src" "resources" "."]
 :deps {
        ;; kotoba.lang.coll replaced clojure.set here (2026-09-24). An explicit
        ;; floor: kotoba.lang.coll carries clojure.set's union/intersection/
        ;; difference/map-invert from 0ffd5f6, and tools.deps takes the NEWEST
        ;; sha it is shown.
        io.github.kotoba-lang/coll {:git/sha "0ffd5f6340af50871d4759b06e02ac4fbf9a4bdf"}
        ;; Declared HERE and not only in deps.edn: nbb reads nbb.edn and does NOT
        ;; read deps.edn or bb.edn, and the .cljs in this repo are run as
        ;; bare `nbb <file>`. A coordinate they cannot see is not a
        ;; declaration. sha and :paths are COPIED from deps.edn.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
        ;; 2026-09-18 (ADR 0086): the whole suite runs on the kbb engine, so the
        ;; six coordinates the tests reach are declared here too. Same shas as
        ;; deps.edn; a drift between the two files is a red in
        ;; `nbb-edn-mirrors-deps-edn-test`.
        io.github.kotoba-lang/osaho {:git/url "https://github.com/kotoba-lang/osaho.git"
                                     :git/sha "1a81e2d7df947a719b75db1e6e5616a900f3ebb7"}
        io.github.kotoba-lang/kotoba-gmir {:git/url "https://github.com/kotoba-lang/kotoba-gmir.git"
                                           :git/sha "cf39f7c1a239666b5e0e3ac82c27bdeb6add0807"}
        io.github.kotoba-lang/kotoba-mir {:git/url "https://github.com/kotoba-lang/kotoba-mir.git"
                                          :git/sha "6d92aea9085914dc013560cc1bb6cf1c171c9c86"}
        io.github.kotoba-lang/kotoba-codegen {:git/url "https://github.com/kotoba-lang/kotoba-codegen.git"
                                              :git/sha "9a54c2496f8d185223ca89b1fc1817f4f9c5bf19"}
        io.github.kotoba-lang/kotoba-object {:git/url "https://github.com/kotoba-lang/kotoba-object.git"
                                             :git/sha "9b53a444cfe5550af6b76eef494b4190b5e4e461"}
        io.github.kotoba-lang/artifact {:git/url "https://github.com/kotoba-lang/artifact.git"
                                        :git/sha "af43fc8fa43edb12361828e26456f75b12ab40be"}}}
